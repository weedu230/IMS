{
  "version": 2,
  "projects": [
    {
      "src": "backend",
      "use": "@vercel/node",
      "config": { "maxDuration": 60 }
    },
    {
      "src": "frontend",
      "use": "@vercel/static-build",
      "config": { "distDir": "build" }
    }
  ]
}
